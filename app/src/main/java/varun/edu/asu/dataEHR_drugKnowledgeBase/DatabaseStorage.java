package varun.edu.asu.dataEHR_drugKnowledgeBase;
import java.util.ArrayList;

import android.database.Cursor;
import android.util.Log;
/**
 * Created by Varun on 12/1/2015.
 */
public class DatabaseStorage {
    /*ArrayList<String> myDataList= new ArrayList<String>();
    //myDataList= new ArrayList<String>();

    myDataList.add("Item1");
    myDataList.add("Item2");
    myDataList.add("Item3");
    myDataList.add("Item4");
    myDataList.add("Item5");

    DatabaseHandler db = new DatabaseHandler();

    db.addListItem(myDataList);

    Cursor cursor = db.getListItem();

    Log.e("count", " " + cursor.getCount());
    if (cursor != null) {
        cursor.moveToNext();

        do {

            Log.e("value==", "" + cursor.getString(1));

        } while (cursor.moveToNext());
    }
}*/
}
